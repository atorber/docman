---
description: 保持前端全局样式与交互一致性
globs: web/src/**/*.{ts,tsx,css,less,scss}
alwaysApply: false
---

# Frontend Consistency

在修改前端页面、组件、样式时，必须优先保持全局一致性，避免引入“单页特例”。

- 使用现有设计体系（Ant Design 组件、项目已有间距/字号/圆角/色彩）优先于自定义样式。
- 同类页面保持统一的布局与交互：列表页、详情页、表单页、侧边栏、弹窗的结构和行为应一致。
- 文案与反馈风格统一：按钮文案、占位符、错误提示、成功提示、空状态提示保持同一语气和格式。
- 状态行为一致：加载、禁用、校验、错误、空数据状态使用项目已有模式，不新增风格冲突的变体。
- 修改组件样式前，先对照同类组件实现；若需破例，需在代码中简短说明原因和适用范围。

## 快速检查清单

- 该改动是否复用了同类页面/组件的布局与间距？
- 交互反馈（loading/success/error/empty）是否与现有页面一致？
- 是否避免了仅在单个页面生效的临时样式覆盖？
