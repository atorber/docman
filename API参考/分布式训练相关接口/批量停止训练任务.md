## 批量停止训练任务
<!---1、 *必选字段，每一类下的接口按照一定的逻辑顺序排列，如 字典序、实现逻辑。-->

### 接口描述
<!---*必选，接口描述：该接口可以实现的具体功能和使用场景。 -->

本接口用于批量停止训练任务，目前支持PyTorchJob，TFJob。



### 请求结构
<!---实现该API功能的基本语法，包括Version、HTTP Method、URI、Header、Body等必要信息。包含所有必选、非必选的参数。-->
```bash
POST ?action=BatchStopJobs&resourcePoolId=xxxx&queueID=xxxx
Host:aihc.bj.baidubce.com
Authorization:authorization string
ContentType: application/json
X-API-Version: v2    
```


### 请求头域

<!--- 情况1：只用到公共头域，无需再解释。-->

除公共头域外，无其它特殊头域。

<!---情况2：非情况1时，对于没有在语法中出现的特殊头域（如初始化等）和可选头域，可在此处进行重点解释。--> 


### 请求参数
<!--- 情况1：无参数。-->
<!--- 情况2：非情况1时，把参数以列表写出。当参数类型是一个对象时，在《数据类型》中用表格列出。-->


| 参数名称    | 类型  | 是否必选 | 参数位置 | 描述  |                                                 
| ---- | ------ | ---- | ------- | ------- |
| queueID     | String      | 是    | Query参数         | 训练任务所属队列，自运维资源池须填入队列名称，托管资源池须填入队列Id|       
| resourcePoolId   | String                      | 是    | Query参数       | 自运维资源池传递资源池唯一标识（示例：cce-1uji3ib5），托管资源池传递 aihc-serverless|
| jobList |List< object >| 是| Body参数 | 任务 ID 列表数组，每个元素为包含 jobId 的对象|
| jobList[].jobId |String| 是    | Body参数 | 单个任务的唯一标识，非空且唯一|
<br>

### 响应头域
<!--- 情况1：只用到公共头域，无需再解释。-->
除公共头域外，无其它特殊头域。
<!---情况2：非情况1时，对于没有在语法中出现的特殊头域和可选头域，可在此处进行重点解释。--> 


### 响应参数
<!--- 情况1：无参数。-->
<!--- 情况2：非情况1时，把参数以列表写出。当参数类型是一个对象时，在《数据类型》中用表格列出。-->

| 参数名称    | 类型    | 描述 |                                               
| -------- | -------- | ---------- |
| requestId | String| 请求ID |
| result | [StopBatchResult](AIHC/API参考/数据结构.md#StopBatchResult)| 批量停止结果对象 |
<br>

### 请求示例
<!---1、*必选，须分别给出请求示例和响应示例，示例要真实有效。包含所有必选参数，非必选参数选择性列举。--> 
<!---2、关键的代码内容要有注释、敏感信息更要有说明，如用户准备AK/SK、用户名、IP地址等。--> 
```json
{
    "jobList": [
        {
            "jobId": "job-r4sczcz7t99a"
        },
        {
            "jobId": "job-gkixx7kngksu"
        },
        {
            "jobId": "job-koephhtzwebl"
        }
    ]
}
```

### 响应示例

```json
{
    "requestId": "636832d5-0be4-4be2-b042-38b6c082458c",
    "result": {
        "successList": [
            {
                "jobId": "job-r4sczcz7t99a",
                "success": true
            },
            {
                "jobId": "job-gkixx7kngksu",
                "success": true
            },
            {
                "jobId": "job-koephhtzwebl",
                "success": true
            }
        ],
        "failedList": [],
        "successNum": 3,
        "failedNum": 0,
        "success": true
    }
}
```