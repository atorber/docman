## 查询资源池列表
使用以下代码可以查询资源池列表。

```python
import logging
import os

import dotenv

from baidubce.bce_client_configuration import BceClientConfiguration
from baidubce.auth.bce_credentials import BceCredentials
from baidubce.services.aihc.aihc_client import AihcClient
from baidubce.exception import BceHttpClientError, BceServerError

# 加载环境变量（可选，推荐使用）
dotenv.load_dotenv()

logging.basicConfig(level=logging.INFO, format='%(asctime)s - %(levelname)s - %(message)s', force=True)
logging.getLogger().setLevel(logging.INFO)
logging.getLogger("baidubce").setLevel(logging.INFO)
__logger = logging.getLogger(__name__)
__logger.setLevel(logging.INFO)

# 配置认证信息和服务端点
HOST = os.getenv('HOST') or 'https://aihc.bj.baidubce.com'  # 百舸AIHC服务地址
AK = os.getenv('AK') or 'your-access-key-id'                # 您的Access Key ID
SK = os.getenv('SK') or 'your-secret-access-key'            # 您的Secret Access Key

# 创建BCE客户端配置
config = BceClientConfiguration(credentials=BceCredentials(AK, SK), endpoint=HOST)

# 创建 aihc client
aihc_client = AihcClient(config)

# 查询资源池列表
try:
    __logger.info('--------------------------------DescribeResourcePools start--------------------------------')
    # resourcePoolType: 资源池类型：1、通用资源池：common，2、托管资源池：dedicatedV2
    response = aihc_client.resource_pool.DescribeResourcePools(resourcePoolType='common')
    print(response)
except BceHttpClientError as e:
    if isinstance(e.last_error, BceServerError):
        __logger.error('send request failed. Response %s, code: %s, msg: %s'
                        % (e.last_error.status_code, e.last_error.code, str(e.last_error)))
    else:
        __logger.error('send request failed. Unknown exception: %s' % e)
```

> 注意: 根据接口文档去填写具体的访问参数，接口链接为[查询资源池列表](https://cloud.baidu.com/doc/AIHC/s/dmgrw0t8l)

## 查询资源池详情
使用以下代码可以查询资源池详情。

```python
import logging
import os

import dotenv

from baidubce.bce_client_configuration import BceClientConfiguration
from baidubce.auth.bce_credentials import BceCredentials
from baidubce.services.aihc.aihc_client import AihcClient
from baidubce.exception import BceHttpClientError, BceServerError

# 加载环境变量
dotenv.load_dotenv()

logging.basicConfig(level=logging.INFO, format='%(asctime)s - %(levelname)s - %(message)s', force=True)
logging.getLogger().setLevel(logging.INFO)
logging.getLogger("baidubce").setLevel(logging.INFO)
__logger = logging.getLogger(__name__)
__logger.setLevel(logging.INFO)

# 配置认证信息
HOST = os.getenv('HOST') or 'https://aihc.bj.baidubce.com'
AK = os.getenv('AK') or 'your-access-key-id'
SK = os.getenv('SK') or 'your-secret-access-key'

config = BceClientConfiguration(credentials=BceCredentials(AK, SK), endpoint=HOST)
aihc_client = AihcClient(config)

# 查询资源池详情
try:
    __logger.info('-------------------------DescribeResourcePool start--------------------------------')
    resource_pool_id = "cce-xxx" # 替换为实际的资源池ID
    response = aihc_client.resource_pool.DescribeResourcePool(resourcePoolId=resource_pool_id)
    print(response)
except BceHttpClientError as e:
    if isinstance(e.last_error, BceServerError):
        __logger.error('send request failed. Response %s, code: %s, msg: %s'
                        % (e.last_error.status_code, e.last_error.code, str(e.last_error)))
    else:
        __logger.error('send request failed. Unknown exception: %s' % e)
```

> 注意: 根据接口文档去填写具体的访问参数，接口链接为[查询资源池详情](https://cloud.baidu.com/doc/AIHC/s/Wmgrw36zt)

## 查询资源池概览
使用以下代码可以查询资源池概览。

```python
import logging
import os

import dotenv

from baidubce.bce_client_configuration import BceClientConfiguration
from baidubce.auth.bce_credentials import BceCredentials
from baidubce.services.aihc.aihc_client import AihcClient
from baidubce.exception import BceHttpClientError, BceServerError

# 加载环境变量
dotenv.load_dotenv()

logging.basicConfig(level=logging.INFO, format='%(asctime)s - %(levelname)s - %(message)s', force=True)
logging.getLogger().setLevel(logging.INFO)
logging.getLogger("baidubce").setLevel(logging.INFO)
__logger = logging.getLogger(__name__)
__logger.setLevel(logging.INFO)

# 配置认证信息
HOST = os.getenv('HOST') or 'https://aihc.bj.baidubce.com'
AK = os.getenv('AK') or 'your-access-key-id'
SK = os.getenv('SK') or 'your-secret-access-key'

config = BceClientConfiguration(credentials=BceCredentials(AK, SK), endpoint=HOST)
aihc_client = AihcClient(config)

# 查询资源池概览
try:
    __logger.info('--------------------------------DescribeResourcePoolOverview start--------------------------------')
    response = aihc_client.resource_pool.DescribeResourcePoolOverview()
    print(response)
except BceHttpClientError as e:
    if isinstance(e.last_error, BceServerError):
        __logger.error('send request failed. Response %s, code: %s, msg: %s'
                        % (e.last_error.status_code, e.last_error.code, str(e.last_error)))
    else:
        __logger.error('send request failed. Unknown exception: %s' % e)
```

> 注意: 根据接口文档去填写具体的访问参数，接口链接为[查询资源池概览](https://cloud.baidu.com/doc/AIHC/s/Imgrw8356)

## 查询资源池配置
使用以下代码可以查询资源池配置。

```python
import logging
import os

import dotenv

from baidubce.bce_client_configuration import BceClientConfiguration
from baidubce.auth.bce_credentials import BceCredentials
from baidubce.services.aihc.aihc_client import AihcClient
from baidubce.exception import BceHttpClientError, BceServerError

# 加载环境变量
dotenv.load_dotenv()

logging.basicConfig(level=logging.INFO, format='%(asctime)s - %(levelname)s - %(message)s', force=True)
logging.getLogger().setLevel(logging.INFO)
logging.getLogger("baidubce").setLevel(logging.INFO)
__logger = logging.getLogger(__name__)
__logger.setLevel(logging.INFO)

# 配置认证信息
HOST = os.getenv('HOST') or 'https://aihc.bj.baidubce.com'
AK = os.getenv('AK') or 'your-access-key-id'
SK = os.getenv('SK') or 'your-secret-access-key'

config = BceClientConfiguration(credentials=BceCredentials(AK, SK), endpoint=HOST)
aihc_client = AihcClient(config)

# 查询资源池配置
try:
    __logger.info('-----------------DescribeResourcePoolConfiguration start--------------------------------')
    resource_pool_id = "cce-xxx" # 替换为实际的资源池ID
    response = aihc_client.resource_pool.DescribeResourcePoolConfiguration(resourcePoolId=resource_pool_id)
    print(response)
except BceHttpClientError as e:
    if isinstance(e.last_error, BceServerError):
        __logger.error('send request failed. Response %s, code: %s, msg: %s'
                        % (e.last_error.status_code, e.last_error.code, str(e.last_error)))
    else:
        __logger.error('send request failed. Unknown exception: %s' % e)
```

> 注意: 根据接口文档去填写具体的访问参数，接口链接为[查询资源池配置](https://cloud.baidu.com/doc/AIHC/s/mmgrw76pp)

## 查询队列列表
使用以下代码可以查询队列列表。

```python
import logging
import os

import dotenv

from baidubce.bce_client_configuration import BceClientConfiguration
from baidubce.auth.bce_credentials import BceCredentials
from baidubce.services.aihc.aihc_client import AihcClient
from baidubce.exception import BceHttpClientError, BceServerError

# 加载环境变量
dotenv.load_dotenv()

logging.basicConfig(level=logging.INFO, format='%(asctime)s - %(levelname)s - %(message)s', force=True)
logging.getLogger().setLevel(logging.INFO)
logging.getLogger("baidubce").setLevel(logging.INFO)
__logger = logging.getLogger(__name__)
__logger.setLevel(logging.INFO)

# 配置认证信息
HOST = os.getenv('HOST') or 'https://aihc.bj.baidubce.com'
AK = os.getenv('AK') or 'your-access-key-id'
SK = os.getenv('SK') or 'your-secret-access-key'

config = BceClientConfiguration(credentials=BceCredentials(AK, SK), endpoint=HOST)
aihc_client = AihcClient(config)

# 查询队列列表
try:
    __logger.info('--------------------------------DescribeQueues start--------------------------------')
    resource_pool_id = "cce-xxx" # 替换为实际的资源池ID
    response = aihc_client.resource_pool.DescribeQueues(resourcePoolId=resource_pool_id)
    print(response)
except BceHttpClientError as e:
    if isinstance(e.last_error, BceServerError):
        __logger.error('send request failed. Response %s, code: %s, msg: %s'
                        % (e.last_error.status_code, e.last_error.code, str(e.last_error)))
    else:
        __logger.error('send request failed. Unknown exception: %s' % e)
```

> 注意: 根据接口文档去填写具体的访问参数，接口链接为[查询队列列表](https://cloud.baidu.com/doc/AIHC/s/vmfcjld9v)

## 查询队列详情
使用以下代码可以查询队列详情。

```python
import logging
import os

import dotenv

from baidubce.bce_client_configuration import BceClientConfiguration
from baidubce.auth.bce_credentials import BceCredentials
from baidubce.services.aihc.aihc_client import AihcClient
from baidubce.exception import BceHttpClientError, BceServerError

# 加载环境变量
dotenv.load_dotenv()

logging.basicConfig(level=logging.INFO, format='%(asctime)s - %(levelname)s - %(message)s', force=True)
logging.getLogger().setLevel(logging.INFO)
logging.getLogger("baidubce").setLevel(logging.INFO)
__logger = logging.getLogger(__name__)
__logger.setLevel(logging.INFO)

# 配置认证信息
HOST = os.getenv('HOST') or 'https://aihc.bj.baidubce.com'
AK = os.getenv('AK') or 'your-access-key-id'
SK = os.getenv('SK') or 'your-secret-access-key'

config = BceClientConfiguration(credentials=BceCredentials(AK, SK), endpoint=HOST)
aihc_client = AihcClient(config)

# 查询队列详情
try:
    __logger.info('--------------------------------DescribeQueue start--------------------------------')
    queue_id = "queue-xxx" # 替换为实际的队列ID
    response = aihc_client.resource_pool.DescribeQueue(queueId=queue_id)
    print(response)
except BceHttpClientError as e:
    if isinstance(e.last_error, BceServerError):
        __logger.error('send request failed. Response %s, code: %s, msg: %s'
                        % (e.last_error.status_code, e.last_error.code, str(e.last_error)))
    else:
        __logger.error('send request failed. Unknown exception: %s' % e)
```

> 注意: 根据接口文档去填写具体的访问参数，接口链接为[查询队列详情](https://cloud.baidu.com/doc/AIHC/s/Jmfcjhru0)