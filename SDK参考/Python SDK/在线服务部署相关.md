## 拉取服务列表
使用以下代码可以拉取服务列表。

```python
import logging
import os
import json

import dotenv

from baidubce.bce_client_configuration import BceClientConfiguration
from baidubce.auth.bce_credentials import BceCredentials
from baidubce.services.aihc.aihc_client import AihcClient
from baidubce.exception import BceHttpClientError, BceServerError

# 加载环境变量（可选，推荐使用）
dotenv.load_dotenv()

# 配置日志
logging.basicConfig(level=logging.INFO, format='%(asctime)s - %(levelname)s - %(message)s', force=True)
logging.getLogger().setLevel(logging.INFO)
logging.getLogger("baidubce").setLevel(logging.INFO)
__logger = logging.getLogger(__name__)
__logger.setLevel(logging.INFO)

# 配置认证信息和服务端点
HOST = os.getenv('HOST') or 'https://aihc.bj.baidubce.com'  # 百舸AIHC服务地址
AK = os.getenv('AK') or 'your-access-key-id'                # 您的Access Key ID
SK = os.getenv('SK') or 'your-secret-access-key'            # 您的Secret Access Key

# 创建BCE客户端配置
config = BceClientConfiguration(credentials=BceCredentials(AK, SK), endpoint=HOST)

# 创建 AihcClient 实例
aihc_client = AihcClient(config)

# 调用接口
try:
    __logger.info('--------------------------------DescribeServices start--------------------------------')
    page_number = 1
    page_size = 10
    order_by = "createdAt"
    order = "asc"
    
    response = aihc_client.service.DescribeServices(
        pageNumber=page_number,
        pageSize=page_size,
        orderBy=order_by,
        order=order
    )
    print(json.dumps(response.__dict__, default=lambda o: o.__dict__, ensure_ascii=False))
except BceHttpClientError as e:
    if isinstance(e.last_error, BceServerError):
        __logger.error('send request failed. Response %s, code: %s, msg: %s'
                       % (e.last_error.status_code, e.last_error.code, str(e.last_error)))
    else:
        __logger.error('send request failed. Unknown exception: %s' % e)
```
> 注意: 根据接口文档去填写具体的访问参数，接口链接为[拉取服务列表](https://cloud.baidu.com/doc/AIHC/s/Imb4v5905)

## 查询服务详情
使用以下代码可以查询服务详情。

```python
import logging
import os
import json

import dotenv

from baidubce.bce_client_configuration import BceClientConfiguration
from baidubce.auth.bce_credentials import BceCredentials
from baidubce.services.aihc.aihc_client import AihcClient
from baidubce.exception import BceHttpClientError, BceServerError

# 加载环境变量（可选，推荐使用）
dotenv.load_dotenv()

# 配置日志
logging.basicConfig(level=logging.INFO, format='%(asctime)s - %(levelname)s - %(message)s', force=True)
logging.getLogger().setLevel(logging.INFO)
logging.getLogger("baidubce").setLevel(logging.INFO)
__logger = logging.getLogger(__name__)
__logger.setLevel(logging.INFO)

# 配置认证信息和服务端点
HOST = os.getenv('HOST') or 'https://aihc.bj.baidubce.com'  # 百舸AIHC服务地址
AK = os.getenv('AK') or 'your-access-key-id'                # 您的Access Key ID
SK = os.getenv('SK') or 'your-secret-access-key'            # 您的Secret Access Key

# 创建BCE客户端配置
config = BceClientConfiguration(credentials=BceCredentials(AK, SK), endpoint=HOST)

# 创建 AihcClient 实例
aihc_client = AihcClient(config)

# 调用接口
try:
    __logger.info('--------------------------------DescribeService start--------------------------------')
    service_id = "s-xxxxxxxx"  # 替换为实际的服务 ID
    response = aihc_client.service.DescribeService(serviceId=service_id)
    print(json.dumps(response.__dict__, default=lambda o: o.__dict__, ensure_ascii=False))
except BceHttpClientError as e:
    if isinstance(e.last_error, BceServerError):
        __logger.error('send request failed. Response %s, code: %s, msg: %s'
                       % (e.last_error.status_code, e.last_error.code, str(e.last_error)))
    else:
        __logger.error('send request failed. Unknown exception: %s' % e)
```
> 注意: 根据接口文档去填写具体的访问参数，接口链接为[查询服务详情](https://cloud.baidu.com/doc/AIHC/s/4mb4v7wn5)

## 获取服务状态
使用以下代码可以获取服务状态。

```python
import logging
import os
import json

import dotenv

from baidubce.bce_client_configuration import BceClientConfiguration
from baidubce.auth.bce_credentials import BceCredentials
from baidubce.services.aihc.aihc_client import AihcClient
from baidubce.exception import BceHttpClientError, BceServerError

# 加载环境变量（可选，推荐使用）
dotenv.load_dotenv()

# 配置日志
logging.basicConfig(level=logging.INFO, format='%(asctime)s - %(levelname)s - %(message)s', force=True)
logging.getLogger().setLevel(logging.INFO)
logging.getLogger("baidubce").setLevel(logging.INFO)
__logger = logging.getLogger(__name__)
__logger.setLevel(logging.INFO)

# 配置认证信息和服务端点
HOST = os.getenv('HOST') or 'https://aihc.bj.baidubce.com'  # 百舸AIHC服务地址
AK = os.getenv('AK') or 'your-access-key-id'                # 您的Access Key ID
SK = os.getenv('SK') or 'your-secret-access-key'            # 您的Secret Access Key

# 创建BCE客户端配置
config = BceClientConfiguration(credentials=BceCredentials(AK, SK), endpoint=HOST)

# 创建 AihcClient 实例
aihc_client = AihcClient(config)

# 调用接口
try:
    __logger.info('-------------------------DescribeServiceStatus start--------------------------------')
    service_id = "s-xxxxxxxx"  # 替换为实际的服务 ID
    response = aihc_client.service.DescribeServiceStatus(serviceId=service_id)
    print(json.dumps(response.__dict__, default=lambda o: o.__dict__, ensure_ascii=False))
except BceHttpClientError as e:
    if isinstance(e.last_error, BceServerError):
        __logger.error('send request failed. Response %s, code: %s, msg: %s'
                       % (e.last_error.status_code, e.last_error.code, str(e.last_error)))
    else:
        __logger.error('send request failed. Unknown exception: %s' % e)
```
> 注意: 根据接口文档去填写具体的访问参数，接口链接为[获取服务状态](https://cloud.baidu.com/doc/AIHC/s/dmb4v6oh0)

## 创建在线服务
使用以下代码可以创建在线服务。

```python
import logging
import os
import json

import dotenv

from baidubce.bce_client_configuration import BceClientConfiguration
from baidubce.auth.bce_credentials import BceCredentials
from baidubce.services.aihc.aihc_client import AihcClient
from baidubce.exception import BceHttpClientError, BceServerError
from baidubce.services.aihc.modules.service.service_model import ResourcePoolConf, ImageConf, ContainerConf

# 加载环境变量（可选，推荐使用）
dotenv.load_dotenv()

# 配置日志
logging.basicConfig(level=logging.INFO, format='%(asctime)s - %(levelname)s - %(message)s', force=True)
logging.getLogger().setLevel(logging.INFO)
logging.getLogger("baidubce").setLevel(logging.INFO)
__logger = logging.getLogger(__name__)
__logger.setLevel(logging.INFO)

# 配置认证信息和服务端点
HOST = os.getenv('HOST') or 'https://aihc.bj.baidubce.com'  # 百舸AIHC服务地址
AK = os.getenv('AK') or 'your-access-key-id'                # 您的Access Key ID
SK = os.getenv('SK') or 'your-secret-access-key'            # 您的Secret Access Key

# 创建BCE客户端配置
config = BceClientConfiguration(credentials=BceCredentials(AK, SK), endpoint=HOST)

# 创建 AihcClient 实例
aihc_client = AihcClient(config)

# 调用接口
try:
    __logger.info('--------------------------------CreateService start--------------------------------')
    # 构造资源池配置
    resource_pool_conf = ResourcePoolConf(
        resourcePoolId="cce-xxx",
        queueName='default',
        resourcePoolType="",
        resourcePoolName="pool-xxx"
    )
    
    # 构造镜像配置
    image_conf = ImageConf(
        imageType=1,
        imageUrl="registry.baidubce.com/csm-online/sglang-router:0.2.0",
    )
    
    # 构造容器配置
    containers = [ContainerConf(
        name="container-test",
        cpus=16,
        memory=64,
        acceleratorCount=1,
        image=image_conf,
        command=[
            "python", "-m", "sglang_router.launch_router",
            "--host", "0.0.0.0", "--port", "30000"
        ],
    )]

    # 创建服务
    response = aihc_client.service.CreateService(
        serviceConf={
            "name": "pythonsdk-aihc-service-test",
            "acceleratorType": "NVIDIA H20-3e",
            "workloadType": "",
            "instanceCount": 1,
            "resourcePool": resource_pool_conf,
            "containers": containers
        }
    )
    print(json.dumps(response.__dict__, default=lambda o: o.__dict__, ensure_ascii=False))
except BceHttpClientError as e:
    if isinstance(e.last_error, BceServerError):
        __logger.error('send request failed. Response %s, code: %s, msg: %s'
                       % (e.last_error.status_code, e.last_error.code, str(e.last_error)))
    else:
        __logger.error('send request failed. Unknown exception: %s' % e)
```
> 注意: 根据接口文档去填写具体的访问参数，接口链接为[创建在线服务](https://cloud.baidu.com/doc/AIHC/s/Imb4uf20u)

## 删除在线服务
使用以下代码可以删除在线服务。

```python
import logging
import os
import json

import dotenv

from baidubce.bce_client_configuration import BceClientConfiguration
from baidubce.auth.bce_credentials import BceCredentials
from baidubce.services.aihc.aihc_client import AihcClient
from baidubce.exception import BceHttpClientError, BceServerError

# 加载环境变量（可选，推荐使用）
dotenv.load_dotenv()

# 配置日志
logging.basicConfig(level=logging.INFO, format='%(asctime)s - %(levelname)s - %(message)s', force=True)
logging.getLogger().setLevel(logging.INFO)
logging.getLogger("baidubce").setLevel(logging.INFO)
__logger = logging.getLogger(__name__)
__logger.setLevel(logging.INFO)

# 配置认证信息和服务端点
HOST = os.getenv('HOST') or 'https://aihc.bj.baidubce.com'  # 百舸AIHC服务地址
AK = os.getenv('AK') or 'your-access-key-id'                # 您的Access Key ID
SK = os.getenv('SK') or 'your-secret-access-key'            # 您的Secret Access Key

# 创建BCE客户端配置
config = BceClientConfiguration(credentials=BceCredentials(AK, SK), endpoint=HOST)

# 创建 AihcClient 实例
aihc_client = AihcClient(config)

# 调用接口
try:
    __logger.info('----------------DeleteService start-----------------------------')
    service_id = "s-xxxxxxxx"
    response = aihc_client.service.DeleteService(serviceId=service_id)
    print(json.dumps(response.__dict__, default=lambda o: o.__dict__, ensure_ascii=False))
except BceHttpClientError as e:
    if isinstance(e.last_error, BceServerError):
        __logger.error('send request failed. Response %s, code: %s, msg: %s'
                       % (e.last_error.status_code, e.last_error.code, str(e.last_error)))
    else:
        __logger.error('send request failed. Unknown exception: %s' % e)
```
> 注意: 根据接口文档去填写具体的访问参数，接口链接为[删除在线服务](https://cloud.baidu.com/doc/AIHC/s/qmb4vfb68)

## 升级在线服务
使用以下代码可以升级在线服务。

```python
import logging
import os
import json

import dotenv

from baidubce.bce_client_configuration import BceClientConfiguration
from baidubce.auth.bce_credentials import BceCredentials
from baidubce.services.aihc.aihc_client import AihcClient
from baidubce.exception import BceHttpClientError, BceServerError
from baidubce.services.aihc.modules.service.service_model import ResourcePoolConf, ImageConf, ContainerConf, ModifyServiceConf

# 加载环境变量（可选，推荐使用）
dotenv.load_dotenv()

# 配置日志
logging.basicConfig(level=logging.INFO, format='%(asctime)s - %(levelname)s - %(message)s', force=True)
logging.getLogger().setLevel(logging.INFO)
logging.getLogger("baidubce").setLevel(logging.INFO)
__logger = logging.getLogger(__name__)
__logger.setLevel(logging.INFO)

# 配置认证信息和服务端点
HOST = os.getenv('HOST') or 'https://aihc.bj.baidubce.com'  # 百舸AIHC服务地址
AK = os.getenv('AK') or 'your-access-key-id'                # 您的Access Key ID
SK = os.getenv('SK') or 'your-secret-access-key'            # 您的Secret Access Key

# 创建BCE客户端配置
config = BceClientConfiguration(credentials=BceCredentials(AK, SK), endpoint=HOST)

# 创建 AihcClient 实例
aihc_client = AihcClient(config)

# 调用接口
try:
    __logger.info('--------------------------------ModifyService start--------------------------------')
    service_id = "s-xxxxxxxx"
    
    # 构造更新后的配置
    resource_pool_conf = ResourcePoolConf(
        resourcePoolId="cce-xxx",
        queueName='default',
        resourcePoolType="",
        resourcePoolName="pool-xxx"
    )
    
    service_conf = ModifyServiceConf(
        name="service-test-updated",
        acceleratorType="NVIDIA H20-3e",
        instanceCount=3,
        resourcePool=resource_pool_conf,
        containers=[ContainerConf(
            name="custom-container",
            cpus=16,
            memory=64,
            acceleratorCount=1,
            image=ImageConf(
                imageType=1,
                imageUrl="registry.baidubce.com/csm-online/sglang-router:0.2.0",
            ),
            command=["python", "launch.py"]
        )]
    )
    
    response = aihc_client.service.ModifyService(serviceId=service_id, serviceConf=service_conf)
    print(json.dumps(response.__dict__, default=lambda o: o.__dict__, ensure_ascii=False))
except BceHttpClientError as e:
    if isinstance(e.last_error, BceServerError):
        __logger.error('send request failed. Response %s, code: %s, msg: %s'
                       % (e.last_error.status_code, e.last_error.code, str(e.last_error)))
    else:
        __logger.error('send request failed. Unknown exception: %s' % e)
```
> 注意: 根据接口文档去填写具体的访问参数，接口链接为[升级在线服务](https://cloud.baidu.com/doc/AIHC/s/Zmb4v974k)

## 服务扩缩容
使用以下代码可以服务扩缩容。

```python
import logging
import os
import json

import dotenv

from baidubce.bce_client_configuration import BceClientConfiguration
from baidubce.auth.bce_credentials import BceCredentials
from baidubce.services.aihc.aihc_client import AihcClient
from baidubce.exception import BceHttpClientError, BceServerError

# 加载环境变量（可选，推荐使用）
dotenv.load_dotenv()

# 配置日志
logging.basicConfig(level=logging.INFO, format='%(asctime)s - %(levelname)s - %(message)s', force=True)
logging.getLogger().setLevel(logging.INFO)
logging.getLogger("baidubce").setLevel(logging.INFO)
__logger = logging.getLogger(__name__)
__logger.setLevel(logging.INFO)

# 配置认证信息和服务端点
HOST = os.getenv('HOST') or 'https://aihc.bj.baidubce.com'  # 百舸AIHC服务地址
AK = os.getenv('AK') or 'your-access-key-id'                # 您的Access Key ID
SK = os.getenv('SK') or 'your-secret-access-key'            # 您的Secret Access Key

# 创建BCE客户端配置
config = BceClientConfiguration(credentials=BceCredentials(AK, SK), endpoint=HOST)

# 创建 AihcClient 实例
aihc_client = AihcClient(config)

# 调用接口
try:
    __logger.info('-----------------------ModifyServiceReplicas start-----------------------------')
    service_id = "s-xxxxxxxx"
    instance_count = 2  # 目标实例数
    
    response = aihc_client.service.ModifyServiceReplicas(
        serviceId=service_id, 
        instanceCount=instance_count
    )
    print(json.dumps(response.__dict__, default=lambda o: o.__dict__, ensure_ascii=False))
except BceHttpClientError as e:
    if isinstance(e.last_error, BceServerError):
        __logger.error('send request failed. Response %s, code: %s, msg: %s'
                       % (e.last_error.status_code, e.last_error.code, str(e.last_error)))
    else:
        __logger.error('send request failed. Unknown exception: %s' % e)
```
> 注意: 根据接口文档去填写具体的访问参数，接口链接为[服务扩缩容](https://cloud.baidu.com/doc/AIHC/s/4mb4vaipm)

## 拉取服务pod列表
使用以下代码可以拉取服务pod列表。

```python
import logging
import os
import json

import dotenv

from baidubce.bce_client_configuration import BceClientConfiguration
from baidubce.auth.bce_credentials import BceCredentials
from baidubce.services.aihc.aihc_client import AihcClient
from baidubce.exception import BceHttpClientError, BceServerError

# 加载环境变量（可选，推荐使用）
dotenv.load_dotenv()

# 配置日志
logging.basicConfig(level=logging.INFO, format='%(asctime)s - %(levelname)s - %(message)s', force=True)
logging.getLogger().setLevel(logging.INFO)
logging.getLogger("baidubce").setLevel(logging.INFO)
__logger = logging.getLogger(__name__)
__logger.setLevel(logging.INFO)

# 配置认证信息和服务端点
HOST = os.getenv('HOST') or 'https://aihc.bj.baidubce.com'  # 百舸AIHC服务地址
AK = os.getenv('AK') or 'your-access-key-id'                # 您的Access Key ID
SK = os.getenv('SK') or 'your-secret-access-key'            # 您的Secret Access Key

# 创建BCE客户端配置
config = BceClientConfiguration(credentials=BceCredentials(AK, SK), endpoint=HOST)

# 创建 AihcClient 实例
aihc_client = AihcClient(config)

# 调用接口
try:
    __logger.info('--------------------------DescribeServicePods start--------------------------------')
    service_id = "s-xxxxxxxx"
    response = aihc_client.service.DescribeServicePods(serviceId=service_id)
    print(json.dumps(response.__dict__, default=lambda o: o.__dict__, ensure_ascii=False))
except BceHttpClientError as e:
    if isinstance(e.last_error, BceServerError):
        __logger.error('send request failed. Response %s, code: %s, msg: %s'
                       % (e.last_error.status_code, e.last_error.code, str(e.last_error)))
    else:
        __logger.error('send request failed. Unknown exception: %s' % e)
```
> 注意: 根据接口文档去填写具体的访问参数，接口链接为[拉取服务pod列表](https://cloud.baidu.com/doc/AIHC/s/cmb4vggnt)

## 删除Pod并重建
使用以下代码可以删除Pod并重建。

```python
import logging
import os
import json

import dotenv

from baidubce.bce_client_configuration import BceClientConfiguration
from baidubce.auth.bce_credentials import BceCredentials
from baidubce.services.aihc.aihc_client import AihcClient
from baidubce.exception import BceHttpClientError, BceServerError

# 加载环境变量（可选，推荐使用）
dotenv.load_dotenv()

# 配置日志
logging.basicConfig(level=logging.INFO, format='%(asctime)s - %(levelname)s - %(message)s', force=True)
logging.getLogger().setLevel(logging.INFO)
logging.getLogger("baidubce").setLevel(logging.INFO)
__logger = logging.getLogger(__name__)
__logger.setLevel(logging.INFO)

# 配置认证信息和服务端点
HOST = os.getenv('HOST') or 'https://aihc.bj.baidubce.com'  # 百舸AIHC服务地址
AK = os.getenv('AK') or 'your-access-key-id'                # 您的Access Key ID
SK = os.getenv('SK') or 'your-secret-access-key'            # 您的Secret Access Key

# 创建BCE客户端配置
config = BceClientConfiguration(credentials=BceCredentials(AK, SK), endpoint=HOST)

# 创建 AihcClient 实例
aihc_client = AihcClient(config)

# 调用接口
try:
    __logger.info('--------------------------------DeleteServicePod start--------------------------------')
    service_id = "s-xxxxxxxx"
    instance_id = "pod-xxxxxxxx"  # Pod 实例 ID
    
    response = aihc_client.service.DeleteServicePod(
        serviceId=service_id, 
        instanceId=instance_id
    )
    print(json.dumps(response.__dict__, default=lambda o: o.__dict__, ensure_ascii=False))
except BceHttpClientError as e:
    if isinstance(e.last_error, BceServerError):
        __logger.error('send request failed. Response %s, code: %s, msg: %s'
                       % (e.last_error.status_code, e.last_error.code, str(e.last_error)))
    else:
        __logger.error('send request failed. Unknown exception: %s' % e)
```
> 注意: 根据接口文档去填写具体的访问参数，接口链接为[删除Pod并重建](https://cloud.baidu.com/doc/AIHC/s/Hmb4vjh24)

## 摘除Pod流量
使用以下代码可以摘除Pod流量。

```python
import logging
import os
import json

import dotenv

from baidubce.bce_client_configuration import BceClientConfiguration
from baidubce.auth.bce_credentials import BceCredentials
from baidubce.services.aihc.aihc_client import AihcClient
from baidubce.exception import BceHttpClientError, BceServerError

# 加载环境变量（可选，推荐使用）
dotenv.load_dotenv()

# 配置日志
logging.basicConfig(level=logging.INFO, format='%(asctime)s - %(levelname)s - %(message)s', force=True)
logging.getLogger().setLevel(logging.INFO)
logging.getLogger("baidubce").setLevel(logging.INFO)
__logger = logging.getLogger(__name__)
__logger.setLevel(logging.INFO)

# 配置认证信息和服务端点
HOST = os.getenv('HOST') or 'https://aihc.bj.baidubce.com'  # 百舸AIHC服务地址
AK = os.getenv('AK') or 'your-access-key-id'                # 您的Access Key ID
SK = os.getenv('SK') or 'your-secret-access-key'            # 您的Secret Access Key

# 创建BCE客户端配置
config = BceClientConfiguration(credentials=BceCredentials(AK, SK), endpoint=HOST)

# 创建 AihcClient 实例
aihc_client = AihcClient(config)

# 调用接口
try:
    __logger.info('--------------------------------DisableServicePod start--------------------------------')
    service_id = "s-xxxxxxxx"
    instance_id = "pod-xxxxxxxx"
    block = False  # 是否阻塞等待
    
    response = aihc_client.service.DisableServicePod(
        serviceId=service_id, 
        instanceId=instance_id, 
        block=block
    )
    print(json.dumps(response.__dict__, default=lambda o: o.__dict__, ensure_ascii=False))
except BceHttpClientError as e:
    if isinstance(e.last_error, BceServerError):
        __logger.error('send request failed. Response %s, code: %s, msg: %s'
                       % (e.last_error.status_code, e.last_error.code, str(e.last_error)))
    else:
        __logger.error('send request failed. Unknown exception: %s' % e)
```
> 注意: 根据接口文档去填写具体的访问参数，接口链接为[摘除Pod流量](https://cloud.baidu.com/doc/AIHC/s/5mb4vhz0a)

## 配置公网访问
使用以下代码可以配置公网访问。

```python
import logging
import os
import json

import dotenv

from baidubce.bce_client_configuration import BceClientConfiguration
from baidubce.auth.bce_credentials import BceCredentials
from baidubce.services.aihc.aihc_client import AihcClient
from baidubce.exception import BceHttpClientError, BceServerError

# 加载环境变量（可选，推荐使用）
dotenv.load_dotenv()

# 配置日志
logging.basicConfig(level=logging.INFO, format='%(asctime)s - %(levelname)s - %(message)s', force=True)
logging.getLogger().setLevel(logging.INFO)
logging.getLogger("baidubce").setLevel(logging.INFO)
__logger = logging.getLogger(__name__)
__logger.setLevel(logging.INFO)

# 配置认证信息和服务端点
HOST = os.getenv('HOST') or 'https://aihc.bj.baidubce.com'  # 百舸AIHC服务地址
AK = os.getenv('AK') or 'your-access-key-id'                # 您的Access Key ID
SK = os.getenv('SK') or 'your-secret-access-key'            # 您的Secret Access Key

# 创建BCE客户端配置
config = BceClientConfiguration(credentials=BceCredentials(AK, SK), endpoint=HOST)

# 创建 AihcClient 实例
aihc_client = AihcClient(config)

# 调用接口
try:
    __logger.info('------------------------ModifyServiceNetConfig start-----------------------------')
    service_id = "s-xxxxxxxx"
    public_access = False  # False 关闭，True 开启
    
    response = aihc_client.service.ModifyServiceNetConfig(
        serviceId=service_id, 
        publicAccess=public_access
    )
    print(json.dumps(response.__dict__, default=lambda o: o.__dict__, ensure_ascii=False))
except BceHttpClientError as e:
    if isinstance(e.last_error, BceServerError):
        __logger.error('send request failed. Response %s, code: %s, msg: %s'
                       % (e.last_error.status_code, e.last_error.code, str(e.last_error)))
    else:
        __logger.error('send request failed. Unknown exception: %s' % e)
```
> 注意: 根据接口文档去填写具体的访问参数，接口链接为[配置公网访问](https://cloud.baidu.com/doc/AIHC/s/dmb4vbr2x)

## 获取实例组列表
使用以下代码可以获取实例组列表。

```python
import logging
import os
import json

import dotenv

from baidubce.bce_client_configuration import BceClientConfiguration
from baidubce.auth.bce_credentials import BceCredentials
from baidubce.services.aihc.aihc_client import AihcClient
from baidubce.exception import BceHttpClientError, BceServerError

# 加载环境变量（可选，推荐使用）
dotenv.load_dotenv()

# 配置日志
logging.basicConfig(level=logging.INFO, format='%(asctime)s - %(levelname)s - %(message)s', force=True)
logging.getLogger().setLevel(logging.INFO)
logging.getLogger("baidubce").setLevel(logging.INFO)
__logger = logging.getLogger(__name__)
__logger.setLevel(logging.INFO)

# 配置认证信息和服务端点
HOST = os.getenv('HOST') or 'https://aihc.bj.baidubce.com'  # 百舸AIHC服务地址
AK = os.getenv('AK') or 'your-access-key-id'                # 您的Access Key ID
SK = os.getenv('SK') or 'your-secret-access-key'            # 您的Secret Access Key

# 创建BCE客户端配置
config = BceClientConfiguration(credentials=BceCredentials(AK, SK), endpoint=HOST)

# 创建 AihcClient 实例
aihc_client = AihcClient(config)

# 调用接口
try:
    __logger.info('-------------------DescribeServicePodGroups start--------------------------------')
    service_id = "s-xxxxxxxx"
    response = aihc_client.service.DescribeServicePodGroups(serviceId=service_id)
    print(json.dumps(response.__dict__, default=lambda o: o.__dict__, ensure_ascii=False))
except BceHttpClientError as e:
    if isinstance(e.last_error, BceServerError):
        __logger.error('send request failed. Response %s, code: %s, msg: %s'
                       % (e.last_error.status_code, e.last_error.code, str(e.last_error)))
    else:
        __logger.error('send request failed. Unknown exception: %s' % e)
```
> 注意: 根据接口文档去填写具体的访问参数，接口链接为[获取实例组列表](https://cloud.baidu.com/doc/AIHC/s/Xmb4vkxbb)

## 拉取服务变更记录
使用以下代码可以拉取服务变更记录。

```python
import logging
import os
import json

import dotenv

from baidubce.bce_client_configuration import BceClientConfiguration
from baidubce.auth.bce_credentials import BceCredentials
from baidubce.services.aihc.aihc_client import AihcClient
from baidubce.exception import BceHttpClientError, BceServerError

# 加载环境变量（可选，推荐使用）
dotenv.load_dotenv()

# 配置日志
logging.basicConfig(level=logging.INFO, format='%(asctime)s - %(levelname)s - %(message)s', force=True)
logging.getLogger().setLevel(logging.INFO)
logging.getLogger("baidubce").setLevel(logging.INFO)
__logger = logging.getLogger(__name__)
__logger.setLevel(logging.INFO)

# 配置认证信息和服务端点
HOST = os.getenv('HOST') or 'https://aihc.bj.baidubce.com'  # 百舸AIHC服务地址
AK = os.getenv('AK') or 'your-access-key-id'                # 您的Access Key ID
SK = os.getenv('SK') or 'your-secret-access-key'            # 您的Secret Access Key

# 创建BCE客户端配置
config = BceClientConfiguration(credentials=BceCredentials(AK, SK), endpoint=HOST)

# 创建 AihcClient 实例
aihc_client = AihcClient(config)

# 调用接口
try:
    __logger.info('----------------------DescribeServiceChangelogs start--------------------------------')
    service_id = "s-xxxxxxxx"
    response = aihc_client.service.DescribeServiceChangelogs(serviceId=service_id)
    print(json.dumps(response.__dict__, default=lambda o: o.__dict__, ensure_ascii=False))
except BceHttpClientError as e:
    if isinstance(e.last_error, BceServerError):
        __logger.error('send request failed. Response %s, code: %s, msg: %s'
                       % (e.last_error.status_code, e.last_error.code, str(e.last_error)))
    else:
        __logger.error('send request failed. Unknown exception: %s' % e)
```
> 注意: 根据接口文档去填写具体的访问参数，接口链接为[拉取服务变更记录](https://cloud.baidu.com/doc/AIHC/s/Zmb4vcylp)

## 查询服务变更详情
使用以下代码可以查询服务变更详情。

```python
import logging
import os
import json

import dotenv

from baidubce.bce_client_configuration import BceClientConfiguration
from baidubce.auth.bce_credentials import BceCredentials
from baidubce.services.aihc.aihc_client import AihcClient
from baidubce.exception import BceHttpClientError, BceServerError

# 加载环境变量（可选，推荐使用）
dotenv.load_dotenv()

# 配置日志
logging.basicConfig(level=logging.INFO, format='%(asctime)s - %(levelname)s - %(message)s', force=True)
logging.getLogger().setLevel(logging.INFO)
logging.getLogger("baidubce").setLevel(logging.INFO)
__logger = logging.getLogger(__name__)
__logger.setLevel(logging.INFO)

# 配置认证信息和服务端点
HOST = os.getenv('HOST') or 'https://aihc.bj.baidubce.com'  # 百舸AIHC服务地址
AK = os.getenv('AK') or 'your-access-key-id'                # 您的Access Key ID
SK = os.getenv('SK') or 'your-secret-access-key'            # 您的Secret Access Key

# 创建BCE客户端配置
config = BceClientConfiguration(credentials=BceCredentials(AK, SK), endpoint=HOST)

# 创建 AihcClient 实例
aihc_client = AihcClient(config)

# 调用接口
try:
    __logger.info('--------------------------DescribeServiceChangelog start-----------------------------')
    change_id = "ch-xxxxxxxx"  # 变更 ID
    response = aihc_client.service.DescribeServiceChangelog(changeId=change_id)
    print(json.dumps(response.__dict__, default=lambda o: o.__dict__, ensure_ascii=False))
except BceHttpClientError as e:
    if isinstance(e.last_error, BceServerError):
        __logger.error('send request failed. Response %s, code: %s, msg: %s'
                       % (e.last_error.status_code, e.last_error.code, str(e.last_error)))
    else:
        __logger.error('send request failed. Unknown exception: %s' % e)
```
> 注意: 根据接口文档去填写具体的访问参数，接口链接为[查询服务变更详情](https://cloud.baidu.com/doc/AIHC/s/xmb4ve6i2)