---
name: doc-consistency-verifier
description: 产品帮助文档诊断工具。当用户需要诊断文档质量问题时使用此skill。适用于验证操作手册、用户指南等技术文档的完整性，检查文档与系统一致性、语法错误、错别字、与常识违背、前后文冲突、失效链接等。手动触发。
---

# 文档诊断工具

本skill用于全面诊断产品帮助文档文档质量问题，帮助发现文档中的各种问题以及优化产品帮助文档。

## 适用场景

- 诊断操作手册的完整性和准确性
- 检查产品文档的错误和过时内容
- 验证文档与实际系统的一致性
- 发现语法、格式、逻辑等各类问题

## 诊断标准

文档诊断包含以下 **18个维度**：

### 1. 文档与系统一致性
- 左侧菜单结构、导航路径、入口位置
- 操作步骤的顺序和完整性
- 页面布局、组件样式、截图对比
- 按钮名称、提示文案、错误信息等

### 2. 语法错误
- 句子结构是否完整
- 标点符号使用是否正确
- 语法是否符合规范

### 3. 错别字
- 检查文档中的错别字
- 检查同音字错误
- 检查形近字错误

### 4. 与常识违背
- 检查文档描述是否符合客观事实
- 检查技术参数是否合理
- 检查流程是否符合常理

### 5. 前后文冲突
- 检查同一文档不同章节的描述是否一致
- 检查术语使用是否统一
- 检查前后步骤是否衔接

### 6. 过时信息
- 检查是否包含已废弃的功能/接口
- 检查版本号是否过期
- 检查链接是否仍然有效

### 7. 缺失步骤
- 检查操作步骤是否完整
- 检查是否遗漏关键步骤
- 检查前置条件是否说明

### 8. 敏感信息泄露
- 检查是否包含密码、密钥等敏感信息
- 检查是否包含Token、AK/SK等凭证
- 检查是否包含个人隐私信息

### 9. 失效链接/图片
- 检查超链接是否有效
- 检查图片是否能够正常显示
- 检查引用资源是否存在

### 10. 格式不一致
- 检查标题层级是否规范
- 检查列表样式是否统一
- 检查代码块格式是否一致

### 11. 描述不清晰/有歧义
- 检查描述是否模糊不清
- 检查是否有歧义性表述
- 检查是否缺少必要的解释

### 12. 代码示例错误
- 检查代码语法是否正确
- 检查代码是否可运行
- 检查代码是否缺少必要依赖
- 检查API调用是否过期

### 13. 截图缺失
- 检查关键步骤是否有配图
- 检查截图是否清晰可读
- 检查截图是否反映最新界面

### 14. 版本兼容性
- 检查功能在不同版本的适用性
- 检查版本号描述是否准确
- 检查版本特定功能是否标注清楚

### 15. 冗余内容
- 检查是否有重复描述
- 检查是否有过时说明
- 检查是否有冗余段落

### 16. 缺少提示/警告/注意
- 检查风险操作是否有警告
- 检查重要提示是否遗漏
- 检查注意事项是否完整

### 17. 命名不规范
- 检查文件名是否规范
- 检查标题命名是否一致
- 检查术语命名是否统一

### 18. 文档完整性
- 检查必要章节是否完整
- 检查目录结构是否合理
- 检查必要说明是否齐全

## 使用前提

**使用此skill时，用户必须提供以下信息**：

1. **要诊断的文档**：文档的完整路径
2. **对应操作界面网址**（可选）：如果需要验证与系统的一致性

**可选提供**：
- **自定义检查要求**：用户可以指定特殊的检查点，如特定的文案关键词、必检的UI元素、需要验证的特定功能等
- **需要诊断的维度**：用户可以指定重点诊断哪些维度，默认为全部18个维度

如果用户未提供文档路径，询问用户补充后再继续执行。

## 诊断流程

### 步骤1：准备工作

1. 明确诊断目标：
   - 需要诊断的文档路径
   - 目标系统URL（可选，用于一致性检查）
   - **自定义检查要求**（如有）：用户指定的特殊检查点
   - **需要诊断的维度**：默认为全部18个，用户可指定重点

2. 检查文档是否存在：
   - 如果文档不存在，告知用户并终止任务

3. 准备输出目录：
   - 原始文档位于 `raw/` 目录下
   - 诊断报告输出到 `report/` 目录下，保持与raw下相同的子目录结构
   - 修复后的新文档输出到 `new/` 目录下，保持与raw下相同的子目录结构
   - 过程记录输出到 `timeline/` 目录下，保持与raw下相同的子目录结构
   - 文件名使用时间戳格式：`[原文档名]_YYYYMMDD_HHMMSS_诊断报告.md`、`[原文档名]_YYYYMMDD_HHMMSSnew.md`、`[原文档名]_YYYYMMDD_HHMMSS_timeline.json`
   - 例如：`raw/操作指南/数据集管理/公共数据集/xxx.md` → `report/操作指南/数据集管理/公共数据集/xxx_20260427_143052_诊断报告.md`
   - 例如：`raw/操作指南/数据集管理/公共数据集/xxx.md` → `new/操作指南/数据集管理/公共数据集/xxx_20260427_143052new.md`
   - 例如：`raw/操作指南/数据集管理/公共数据集/xxx.md` → `timeline/操作指南/数据集管理/公共数据集/xxx_20260427_143052_timeline.json`

### 步骤2：读取并分析文档

1. 读取原始文档内容
2. 解析文档结构（标题层级、段落、代码块等）
3. 提取关键信息（菜单路径、按钮名称、操作步骤等）

### 步骤3：执行文档诊断

#### 3.1 基础诊断（不依赖系统）

##### 语法错误检查
- 扫描文档中的语法问题
- 标记可能的错误位置

##### 错别字检查
- 使用语言模型识别常见错别字
- 检查同音字和形近字错误

##### 与常识违背检查
- 检查技术参数是否合理
- 检查描述是否符合客观事实

##### 前后文冲突检查
- 扫描同一文档中的不一致描述
- 检查术语使用是否统一

##### 过时信息检查
- 检查是否包含已废弃的功能描述
- 检查版本信息是否过期

##### 敏感信息泄露检查
- 扫描密码、密钥、Token等敏感词
- 标记可能泄露隐私的内容

##### 失效链接/图片检查
- 提取文档中的链接和图片路径
- 验证链接有效性

##### 格式不一致检查
- 检查标题层级是否规范
- 检查列表样式是否统一

##### 描述不清晰检查
- 识别模糊表述
- 标记可能引起歧义的内容

##### 代码示例错误检查
- 检查代码语法是否正确
- 检查API调用是否过期
- 检查必要依赖是否说明

##### 截图缺失检查
- 检查关键步骤是否有配图
- 检查截图是否清晰可读

##### 版本兼容性检查
- 检查功能在不同版本的适用性描述
- 检查版本号是否标注清楚

##### 冗余内容检查
- 检查是否有重复描述
- 检查是否有过时说明

##### 缺少提示/警告/注意检查
- 检查风险操作是否有警告
- 检查重要提示是否遗漏

##### 命名规范性检查
- 检查文件名是否规范
- 检查标题命名是否一致

##### 文档完整性检查
- 检查必要章节是否完整
- 检查目录结构是否合理

#### 3.2 一致性检查（需要浏览器）

如果用户提供了目标系统URL，执行一致性验证：

1. 调用 `browser-use` skill 打开目标URL
2. 检查登录状态，必要时提示用户登录
3. 逐项对比文档与实际系统

#### 3.3 自定义检查（可选）

如果用户提供了自定义检查要求，额外执行：
- 检查用户指定的特定文案关键词
- 验证用户指定的UI元素
- 按照用户要求重点检查某些场景

### 步骤4：生成诊断报告

诊断完成后，生成诊断报告，格式如下：

```markdown
# 文档诊断报告：[文档名称]

**诊断日期**：YYYY-MM-DD
**诊断维度**：18个维度（文档与系统一致性、语法错误、错别字、与常识违背、前后文冲突、过时信息、缺失步骤、敏感信息泄露、失效链接/图片、格式不一致、描述不清晰/有歧义、代码示例错误、截图缺失、版本兼容性、冗余内容、缺少提示/警告/注意、命名不规范、文档完整性）
**诊断方式**：基础诊断 + 一致性检查（可选）+ 自定义检查（可选）

---

## 一、问题汇总

共发现 **X处问题**，按严重程度分类如下：

### 1. 高优先级问题（X处）

#### 1.1 敏感信息泄露（X处）
[详细描述每一处问题及位置]

#### 1.2 失效链接/图片（X处）
[详细描述每一处问题及位置]

#### 1.3 与系统不一致（X处）
[详细描述每一处问题及位置]

#### 1.4 代码示例错误（X处）
[详细描述每一处问题及位置]

#### 1.5 缺少提示/警告/注意（X处）
[详细描述每一处问题及位置]

### 2. 中优先级问题（X处）

#### 2.1 语法错误（X处）
[详细描述每一处问题及位置]

#### 2.2 错别字（X处）
[详细描述每一处问题及位置]

#### 2.3 前后文冲突（X处）
[详细描述每一处问题及位置]

#### 2.4 过时信息（X处）
[详细描述每一处问题及位置]

#### 2.5 缺失步骤（X处）
[详细描述每一处问题及位置]

#### 2.6 版本兼容性问题（X处）
[详细描述每一处问题及位置]

### 3. 低优先级问题（X处）

#### 3.1 格式不一致（X处）
[详细描述每一处问题及位置]

#### 3.2 描述不清晰（X处）
[详细描述每一处问题及位置]

#### 3.3 截图缺失（X处）
[详细描述每一处问题及位置]

#### 3.4 冗余内容（X处）
[详细描述每一处问题及位置]

#### 3.5 命名不规范（X处）
[详细描述每一处问题及位置]

#### 3.6 文档完整性（X处）
[详细描述每一处问题及位置]

---

## 二、诊断通过项

以下诊断维度未发现问题：

| 诊断项 | 状态 |
|--------|------|
| [诊断项1] | ✅ 通过 |
| [诊断项2] | ✅ 通过 |

---

## 三、修复建议

| 优先级 | 问题 | 修复建议 |
|--------|------|----------|
| **高** | [问题描述] | [修复建议] |
| **中** | [问题描述] | [修复建议] |
| **低** | [问题描述] | [修复建议] |
```

### 步骤5：创建修复后的文档和诊断报告

基于诊断结果，创建以下输出：

1. **修复后的新文档**：
   - 输出到 `new/` 目录，保持与 `raw/` 相同的子目录结构
   - 文件名格式：`[原文档名]_YYYYMMDD_HHMMSSnew.md`（时间戳精确到秒）
   - 修复诊断中发现的所有问题
   - 更新过时的截图和链接

2. **诊断报告**：
   - 输出到 `report/` 目录，保持与raw下相同的子目录结构
   - 文件名格式：`[原文档名]_YYYYMMDD_HHMMSS_诊断报告.md`
   - 时间戳与修复后的文档保持一致，便于匹配同一次诊断

## 输出要求

1. **诊断报告**：输出到 `report/` 目录，保持与raw下相同的子目录结构，格式为 `[原文档名]_YYYYMMDD_HHMMSS_诊断报告.md`
2. **修复后的文档**：输出到 `new/` 目录，保持与raw下相同的子目录结构，格式为 `[原文档名]_YYYYMMDD_HHMMSSnew.md`
3. **过程记录**：输出到 `timeline/` 目录，格式为 `[原文档名]_[时间戳]_timeline.json`
4. **时间戳规则**：同一次诊断生成的文件使用相同的时间戳（精确到秒），便于匹配对应关系
5. 多次诊断运行时，不同时间戳的文件会自动保存为不同的记录
6. 如果需要，输出修复建议

## 诊断进度与过程记录

### 进度展示

在执行诊断过程中，实时展示进度状态：

- 开始诊断时：显示"开始诊断文档：[文档名]"
- 每个诊断维度执行时：显示"正在执行：[维度名称] (X/18)"
- 诊断完成时：显示"诊断完成，发现 X 处问题"

### 错误处理

诊断过程中可能遇到的异常情况及处理方式：

| 异常情况 | 处理方式 |
|----------|----------|
| 文档不存在 | 提示用户"文档不存在，请检查路径"，终止任务 |
| 文件读取失败 | 提示"文件读取失败"，记录错误并继续处理其他维度 |
| 目录创建失败 | 提示"无法创建输出目录"，询问用户是否指定其他位置 |
| 链接验证超时 | 标记为"待验证"，记录在报告中不影响其他检查 |
| 浏览器打开失败 | 跳过一致性检查，提示用户手动验证 |
| 登录超时 | 提示用户重新登录，或跳过一致性检查 |

### 过程记录（Timeline）

每次诊断任务的过程数据保存到 `timeline/` 目录，文件名格式：`[原文档名]_[时间戳]_timeline.json`

#### JSON 结构

```json
{
  "task_id": "唯一标识符",
  "document": {
    "name": "文档名称",
    "path": "原始文档路径"
  },
  "target_url": "目标系统URL（可选）",
  "start_time": "2024-04-27T14:30:52Z",
  "end_time": "2024-04-27T14:32:15Z",
  "duration_seconds": 83,
  "status": "completed | failed | partial",
  "dimensions": [
    {
      "id": 1,
      "name": "文档与系统一致性",
      "status": "pending | running | completed | failed",
      "start_time": "2024-04-27T14:30:52Z",
      "end_time": "2024-04-27T14:30:58Z",
      "duration_ms": 6000,
      "issue_count": 2,
      "issues": [
        {
          "type": "inconsistency",
          "severity": "high",
          "description": "菜单路径描述不完整",
          "location": "步骤1文案"
        }
      ]
    },
    {
      "id": 2,
      "name": "语法错误",
      "status": "completed",
      "start_time": "2024-04-27T14:30:58Z",
      "end_time": "2024-04-27T14:31:02Z",
      "duration_ms": 4000,
      "issue_count": 0,
      "issues": []
    }
  ],
  "summary": {
    "total_issues": 5,
    "by_severity": {
      "high": 2,
      "medium": 2,
      "low": 1
    },
    "by_dimension": {
      "文档与系统一致性": 2,
      "语法错误": 0,
      "错别字": 1,
      "其他维度": 2
    }
  },
  "outputs": {
    "report": "report/操作指南/数据集管理/公共数据集/下载和使用公共数据集_20260427_143052_诊断报告.md",
    "fixed_doc": "new/操作指南/数据集管理/公共数据集/下载和使用公共数据集_20260427_143052new.md"
  },
  "errors": [
    {
      "code": "BROWSER_FAILED",
      "message": "无法打开目标页面",
      "dimension": "文档与系统一致性"
    }
  ]
}
```

#### 字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| task_id | string | 唯一任务标识符 |
| document.name | string | 文档名称（不含路径） |
| document.path | string | 原始文档完整路径 |
| target_url | string | 目标系统URL（可选） |
| start_time | ISO8601 | 任务开始时间 |
| end_time | ISO8601 | 任务结束时间 |
| duration_seconds | number | 总耗时（秒） |
| status | enum | 任务状态：completed/failed/partial |
| dimensions | array | 各维度诊断详情 |
| dimensions[].id | number | 维度编号（1-18） |
| dimensions[].name | string | 维度名称 |
| dimensions[].status | enum | 执行状态 |
| dimensions[].start_time | ISO8601 | 开始时间 |
| dimensions[].end_time | ISO8601 | 结束时间 |
| dimensions[].duration_ms | number | 耗时（毫秒） |
| dimensions[].issue_count | number | 发现问题数量 |
| dimensions[].issues | array | 问题详情列表 |
| summary | object | 汇总信息 |
| outputs | object | 输出文件路径 |
| errors | array | 错误记录 |

## 注意事项

- 如果页面需要登录，先提示用户登录
- 截图是验证UI一致性的重要依据
- 优先关注高优先级问题（敏感信息、失效链接、系统不一致）
- 保持报告客观、准确
- 敏感信息泄露问题必须高亮提醒